{
    "version": 2,
    "builds": [
      { "src": "src/api/*.ts", "use": "@vercel/node" },
      { "src": "src/**/*.tsx", "use": "@vercel/static-build" }
    ],
    "routes": [
      { "src": "/api/(.*)", "dest": "src/api/$1" },
      { "src": "/(.*)", "dest": "/src/pages/$1" }
    ]
  }